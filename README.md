# Cost_Based_Query_Optimizer
CB-SQLopt is a C/C++ cost-based SQL optimizer with Selinger DP join ordering, four rewrite rules, and per-column statistics. Achieves 100–1000× speedups over naive plans. Includes SQL shell, EXPLAIN, catalog cache, executor, and benchmark. Built for Advanced DBMS course.
